# Project photography

Drop production stills per project using this folder layout:

```
public/assets/projects/{slug}/cover.jpg
public/assets/projects/{slug}/01.jpg
public/assets/projects/{slug}/02.jpg
```

Then set `localCover` and `localGallery` on the matching entry in `src/content/featuredProjects.ts`.
